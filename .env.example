# Copy to .env locally — never commit real secrets.
# Wave 1: orchestrator runs without DATABASE_URL (in-memory tickets + audit).

PORT=8787

# Optional: shared TROZLANIO Postgres (schema trozbot). Leave unset for local/tests.
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME
