# deploy/k8s — Thin Kubernetes

Phase 1: namespace, Deployments (≤3), Services, Ingress, probes, limits, secrets refs.

Admission / signature verification policy lives with cluster policy as it is introduced (see blueprint security baseline).

**Status:** scaffold only — manifests land in Wave 4.
