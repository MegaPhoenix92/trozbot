# EXAMPLE ONLY — do not put real credentials here.
# Create the real secret out-of-band:
#   kubectl -n trozbot create secret generic trozbot-runtime \
#     --from-literal=DATABASE_URL='postgresql://...' \
#     --from-literal=ORCHESTRATOR_URL='http://orchestrator:8787'
# Images pull secrets / registry creds are owner-managed (not in git).
apiVersion: v1
kind: Secret
metadata:
  name: trozbot-runtime
  namespace: trozbot
  labels:
    app.kubernetes.io/part-of: trozbot
type: Opaque
stringData:
  # Placeholders only — replace via sealed-secrets / external-secrets / kubectl create
  DATABASE_URL: "REPLACE_ME_OUT_OF_BAND"
  ORCHESTRATOR_URL: "http://orchestrator:8787"
