# apps/orchestrator — AI orchestration

Phase 1 tools only:

1. Knowledge-base retrieve (read)
2. `create_ticket` (write)

Policy: no risky automations. Session + tool audit → shared TROZLANIO Postgres (`trozbot` schema) + Redis.

**Status:** scaffold only.
