# apps/voice-gateway — Live audio

Phase 1: duplex audio path (STT/TTS edge or stream proxy). One session ↔ one media path.

**Status:** scaffold only. Do not overbuild before orchestrator vertical slice.
